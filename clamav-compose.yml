version: '3'

services:
  esign-clamav-1:
    build:
      context: /home/anduc/kltn/esign-clamav/.
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env.clam
    expose:
      - "7009"
    networks:
      -  esign-dev-network

networks:
  esign-dev-network:
    external: true




    

 